# Tree-sitter WASM grammars

Prebuilt WASM language parsers used by Threadcord's repo map (`src/repomap`).
Sourced from the official tree-sitter-* npm packages (same major versions pinned
in git history when last refreshed). Keep filenames stable — loaders resolve by name.
