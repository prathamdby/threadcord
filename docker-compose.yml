services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: threadcord
      POSTGRES_PASSWORD: threadcord
      POSTGRES_DB: threadcord
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U threadcord -d threadcord"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres-data:/var/lib/postgresql/data

  threadcord:
    build: .
    init: true
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://threadcord:threadcord@postgres:5432/threadcord
      WORKSPACE_ROOT: /workspaces
      # THREADCORD_HTTP_BEARER is supplied via .env (env_file). Production must set a private value.
    ports:
      - "3583:3583"
    volumes:
      - threadcord-workspaces:/workspaces
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3583/health/live').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4g

volumes:
  postgres-data:
  threadcord-workspaces:
